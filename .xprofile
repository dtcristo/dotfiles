#
# ~/.xprofile
#
#

# if which dbus-launch >/dev/null && test -z "$DBUS_SESSION_BUS_ADDRESS"; then
#     eval "$(dbus-launch --sh-syntax --exit-with-session)"
# fi

# Environment variables
#

[[ -f ~/.extend.xprofile ]] && . ~/.extend.xprofile

# Disable system bell
xset -b

# Ctrl acts as Esc, Shift keys swtich spaces in Xfce.
# xcape -t 200 -e 'Control_L=Escape;Shift_L=Control_R|Alt_R|Left;Shift_R=Control_R|Alt_R|Right'
